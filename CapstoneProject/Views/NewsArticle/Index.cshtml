@model List<NewsArticle>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int count = 0;
}

<div class="container-xxl flex-grow-1 container-p-y">
    <div class="card">
      <h5 class="card-header">ALL NEWS ARTICLE</h5>
      <a href="/NewsArticle/Add" class="btn btn-outline-primary offcanvas-title">ADD NEWS ARTICLE</a>
        <br />
      <div class="table-responsive text-nowrap">
        <table class="table">
          <thead>
            <tr>
              <th>#</th>
              <th>TITLE</th>
              <th>DATE</th>
              <th>CATEGORY</th>
              <th>UPDATE</th>
              <th>DELETE</th>
            </tr>
          </thead>
          <tbody class="table-border-bottom-0">
              @foreach (var item in Model)
                    {
                        count++;
                        <tr class="table-active">
                            <td>@count</td>
                            <td><i class="ti ti-brand-react-native ti-lg text-info me-3"></i> <strong>@item.Title</strong></td>
                            <td>@item.Date.ToShortDateString()</td>
                            <td><span class="badge bg-label-success me-1">@item.ArticleCategory.CategoryName</span></td>
                            <td><a href="/NewsArticle/Edit" class="btn btn-outline-primary">Update</a></td>
                            <td><a href="/NewsArticle/Delete" class="btn btn-outline-primary">Delete</a></td>
                        </tr>
                    }
                    
          </tbody>
        </table>       
      </div>
    </div>
</div>

